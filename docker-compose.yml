version: '3.7'
services:
  web:
    build: ./rub2b_dev
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./rub2b_dev:/usr/src/rub2b_dev/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev